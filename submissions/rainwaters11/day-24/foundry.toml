[profile.default]
src             = "contracts"
out             = "out"
libs            = ["lib"]
solc_version    = "0.8.26"
via_ir          = true
optimizer       = true
optimizer_runs  = 200

# No external dependencies — pure Solidity, no OZ imports needed.
# SimpleMultiSig.sol is self-contained for maximum auditability.
